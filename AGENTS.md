# AGENTS.md

## 项目定位

这是一个基于 Tauri 2、React、TypeScript 和 Vite 的 AGENT 工具。
项目需要同时支持桌面端和移动端，所有界面都必须优先考虑窄屏触控、键盘操作、流式输出和长时间运行任务。

## 技术选型

### UI 基础

- 使用 `shadcn/ui` 作为业务组件层，组件源码放在 `src/components/ui/`，需要时可以在项目内修改。
- 使用 Radix UI primitives 作为无障碍交互基础，优先复用其 Dialog、Popover、DropdownMenu、Tooltip、Tabs、ScrollArea 等能力。
- 使用 Tailwind CSS 负责布局、间距、响应式断点和主题变量。
- 使用 `lucide-react` 提供图标。按钮已有文字时不要重复放置相同含义的图标；图标按钮必须设置可读的 `aria-label`。
- 禁止同时引入 Ant Design、Material UI、Chakra UI 等第二套通用组件库，避免样式、交互和包体积失控。

### Agent 功能配套

- 使用 `Zustand` 管理会话选择、侧栏展开状态、草稿、主题和其他本地 UI 状态。
- 使用 `TanStack Query` 管理 Agent、会话、任务和设置等异步数据；不要把服务端数据长期复制到 Zustand。
- 使用 `react-hook-form` 和 `zod` 管理表单与校验。
- 使用 `react-markdown` 配合受控的 remark/rehype 插件渲染 Agent 输出。默认禁止原始 HTML，外部链接必须经过安全处理。
- 代码、日志和工具调用输出使用等宽字体，并提供复制、折叠、换行和滚动能力。
- 图表只有在确实能帮助理解 Agent 结果时才引入；优先使用轻量方案，避免为单个简单指标引入大型图表库。

除非有明确需求，不要引入重量级编辑器、完整办公套件或移动端原生 UI 框架。Tauri 移动端仍使用 WebView，优先保持一套 React UI，通过响应式布局适配平台。

## 目录约定

```text
src/
  components/
    ui/              # shadcn/ui 基础组件
    agent/           # Agent 对话、消息、工具调用和任务组件
    layout/          # 桌面侧栏、移动导航、页面骨架
  features/          # 按业务能力划分的页面和状态
  hooks/             # 可复用 React hooks
  lib/               # 无副作用的通用工具、校验和适配器
  stores/            # Zustand store
  types/             # 跨模块共享类型
  App.tsx
src-tauri/
  src/               # Tauri 命令、窗口和平台能力
```

业务模块应尽量内聚：组件、hooks、store、schema 和 API 适配器优先放在同一个 feature 目录中，只有真正跨模块复用时才上移到公共目录。

## Agent 工作台交互规范

- 对话区是核心内容，桌面端可采用“会话列表 + 主对话 + 详情/工具面板”的三栏结构；移动端默认只显示一个主面板，通过抽屉或底部 Sheet 打开其他面板。
- 输入框必须支持多行、草稿保留、发送快捷键提示、取消生成和重试；移动端不能依赖悬停操作。
- Agent 生成内容必须区分消息、思考状态、工具调用、工具结果、错误和最终答复，不要把所有事件拼成一段纯文本。
- 流式输出期间显示明确的进行中状态；用户切换会话或页面时不能丢失已接收内容。
- 长消息、日志和代码块必须限制最大高度并支持滚动；禁止让无限增长的内容撑开整个页面。
- 危险操作、删除会话、执行外部工具和覆盖文件前必须二次确认，并说明影响范围。
- 工具调用应展示名称、参数摘要、执行状态、耗时和错误信息；敏感参数默认脱敏。
- 空状态要告诉用户下一步能做什么；错误状态要提供可执行的恢复动作，而不是只显示错误码。

## 响应式与平台规范

- 采用移动优先布局。至少验证窄屏手机、平板和桌面宽屏三类尺寸，不要只在开发机窗口中验证。
- 触控目标建议不小于 `44px × 44px`；紧凑桌面按钮可以缩小，但不能让移动端复用同样的尺寸。
- 不要依赖 `hover` 才能完成关键操作；所有核心能力必须可通过触控、键盘和辅助技术完成。
- 桌面端支持侧栏快捷键、拖拽调整宽度和右侧详情面板；移动端将其替换为抽屉、Sheet 或底部导航。
- 使用 CSS safe-area 变量处理刘海、底部手势区和键盘顶起，避免固定底栏遮挡输入框。
- 不在业务组件中硬编码平台判断。平台差异通过响应式 CSS、能力适配器或明确的 `platform` 模块处理。
- 颜色、间距、圆角、阴影和字体统一使用设计 token，不在组件中随意写新的颜色值。
- 亮色和暗色主题都必须可用，并确保代码块、状态色和禁用态在两种主题下有足够对比度。

## Tauri 与安全

- 前端只通过明确的 Tauri command 或插件调用原生能力，不直接假设 Node.js、文件系统或浏览器特权存在。
- 新增 Tauri command 时同步更新 capability 权限，并遵循最小权限原则；不为了方便开启全量权限。
- 文件路径、外部 URL、命令参数和 Agent 工具参数都必须在边界处校验，不能信任模型输出。
- Agent 输出默认视为不可信文本。渲染 Markdown、HTML、链接和代码时必须防止 XSS、路径穿越和命令注入。
- API key、会话令牌和用户隐私数据不能写入日志、前端源码、Git 或错误上报内容。
- 桌面端和移动端能力不一致时，要提供明确的能力检测和降级提示，不得静默失败。

## TypeScript 与 React 规范

- 保持 TypeScript 严格类型，禁止使用 `any` 绕过类型检查；外部数据先校验再进入业务层。
- 组件使用函数组件和 hooks。组件只负责展示与交互编排，网络请求、持久化和平台调用放在 hooks、store 或适配器中。
- 避免过大的组件；当一个组件同时承担布局、数据请求、流式解析和复杂交互时，应按业务职责拆分。
- 事件处理、异步请求和流式订阅必须正确清理，避免组件卸载后更新状态或重复订阅。
- 列表渲染使用稳定且业务唯一的 `key`，不要使用数组下标作为可变列表的 key。
- 不要为了抽象而抽象。先保证 Agent 工作流清晰，再提取真正复用的组件和 hooks。
- 单行 TypeScript 代码不要超过 120 个字符；长参数、对象和 JSX 属性应分行书写。

## 函数注释与 help 文件

- 新建函数时，函数上方必须使用中文注释说明用途。
- 函数超过 20 行时，函数内部必须使用中文 `step` 注释描述业务逻辑。
- `step` 描述业务层级，不描述简单的 `if`、循环或执行顺序。使用 `step.1`、`step.1.1` 这样的层级。
- 一个函数包含多个业务层级时，评估是否可以把实现细节提取到当前目录的 `help.ts` 文件。
- 提取的辅助函数命名必须以 `help_` 开头，只允许当前目录的代码调用，禁止跨目录调用。
- `help.ts` 中的函数同样必须遵守中文函数注释和 `step` 注释规则。
- 业务主函数应保留清晰的业务流程，复杂的序列化、解析、平台调用和错误转换应隐藏在 `help_*` 函数中。

## 测试与验收

每次完成一项功能后，至少执行：

```bash
npm run build
cargo check --manifest-path src-tauri/Cargo.toml
```

涉及 Agent 流式输出、工具调用、权限、文件操作或响应式布局时，还应补充对应测试或手工验收：

- 流式输出可开始、暂停/取消、完成、重试和恢复错误。
- 会话切换不会串消息，应用重启后的持久化行为符合预期。
- 工具调用权限被拒绝时有明确提示，异常不会泄露敏感参数。
- 手机窄屏、软键盘弹出、横竖屏切换和桌面窗口缩放时布局不溢出。
- 键盘导航、焦点顺序、屏幕阅读器名称和颜色对比度可用。

提交前检查 `git diff`，不要提交 `node_modules`、构建产物、密钥、临时日志或本地数据库。

## 开发命令

```bash
npm install
npm run dev
npm run tauri dev
npm run build
cargo check --manifest-path src-tauri/Cargo.toml
```

项目根目录的 `.cargo/config.toml` 已将 Cargo 编译缓存固定到主机的 `/tmp/hh-tauri-target`。
该目录只存放可重新生成的 Rust 编译缓存，不存放源码、密钥或业务数据，也不提交到 Git。
执行 `cargo clean` 前先关闭编辑器的 Rust Analyzer 检查，避免它占用 target 文件；开发和检查命令之间也不要并发操作同一个 target 目录。

```bash
npm run tauri dev
```

Rust 依赖首次编译较慢属于正常现象；不要让多个 `cargo check`、`cargo run` 或 `tauri dev` 同时操作同一个 target 目录。
